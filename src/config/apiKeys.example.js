/**
 * API Keys — edit apiKeys.js and replace the placeholder with your real key.
 *
 * IMPORTANT: If you add a real key, uncomment the gitignore line for
 * src/config/apiKeys.js so you don't accidentally commit your key.
 *
 * To get an Anthropic API key:
 * 1. Go to https://console.anthropic.com
 * 2. Create an account and add credits
 * 3. Go to API Keys and create a new key
 * 4. Paste it in apiKeys.js
 */

export const ANTHROPIC_API_KEY = 'your-api-key-here';
