/**
 * API Keys — replace the placeholder with your real API key.
 * See apiKeys.example.js for setup instructions.
 *
 * IMPORTANT: If you add a real key here, uncomment the gitignore line
 * for this file so you don't accidentally commit your key.
 */

export const ANTHROPIC_API_KEY = 'your-api-key-here';
